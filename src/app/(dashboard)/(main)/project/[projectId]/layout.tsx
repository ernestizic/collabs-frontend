import ProjectInitializer from "./_components/ProjectInitializer";

const Layout = ({ children }: { children: React.ReactNode }) => {
	return <ProjectInitializer>{children}</ProjectInitializer>;
};

export default Layout;
